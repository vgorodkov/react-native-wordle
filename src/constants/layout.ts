import { moderateScale, scale, verticalScale } from 'utils/metrics';

export const Layout = {
  largeSpacing: 48,
  mediumSpacing: 24,
  defaultSpacing: 16,
  smallSpacing: 8,
  keyboardLetterSpacing: 10,
  wordBox: 64,
  defaultIconSize: 32,
};
